<UserControl x:Class="FriendOrganizer.UI.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FriendOrganizer.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding Friends}"                   
                  DisplayMemberPath="DisplayMember"
                  Grid.Column="0" />
        <!--"Grid.Colum="0" - Also defaule value that we dont need to specify; "TwoWay" - value is also writen back to viewModel whe item is changed "DisplayMemberPath" - property that we want to display-->
        <!--SelectedItem="{Binding SelectedFriend, Mode=TwoWay}" - OBSOLITE-->
    </Grid>
</UserControl>
